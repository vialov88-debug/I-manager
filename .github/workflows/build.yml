name: Build APK
on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'
          
      - name: Install Flet
        run: |
          pip install flet
          
      - name: Setup Java
        uses: actions/setup-java@v3
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Build Android APK
        run: |
          # --yes отвечает "да" на установку Flutter
          # --verbose показывает детальный процесс
          flet build apk --yes --verbose
          
      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: SoundTool-APK
          path: build/apk/*.apk
